import styles from '@/styles/divider/HorizontalDivider.module.scss';

type Props = {
};

const HorizontalDivider = ({}: Props) => {

  return (
    <div className={styles.main}>

    </div>
  );
};

export default HorizontalDivider;
